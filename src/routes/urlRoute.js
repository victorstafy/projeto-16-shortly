import express from 'express';
// import { test1, test2,test3, test4 } from '../controllers/urlController.js';

const router = express.Router();

// router.post('/urls/shorten', test1);
// router.get('/urls/:id', test2);
// router.get('/urls/open/:shortUrl', test3);
// router.delete('/urls/:id', test4);

export default router;